fn main() -> i32 {
    let a: bool = true;
    let b: bool = false;

    // true && false = false
    if a && b {
        return 1;
    }

    // true || false = true
    if a || b {
        return 42;
    }

    return 0;
}
